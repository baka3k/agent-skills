# Architecture Overview — {ProjectName}

> Evidence sources: `{mind_mcp_refs}`, `{graph_mcp_refs}`, `{filesystem_refs}`

## System Architecture

```mermaid
graph TD
    %% Auto-generated from graph_mcp module exploration
    {mermaid_diagram}
```

## High-Level Modules

| Module | Type | Responsibility | Key Entry Points |
|--------|------|----------------|------------------|
| `{module_name}` | `{type}` | `{responsibility}` | `{entry_points}` |

## Module Dependency Graph

| Module | Depends On | Used By |
|--------|------------|---------|
| `{module_name}` | `{dependencies}` | `{dependents}` |

## Design Patterns

<!-- Extracted from mind_mcp architecture docs -->
- **Pattern**: `{pattern_name}` — used in `{module_name}` for `{purpose}`

## Technology Stack

| Layer | Technology | Evidence |
|-------|-----------|----------|
| `{layer}` | `{tech}` | `{source}` |

## Architecture Decisions

<!-- From mind_mcp ADR/docs -->
| Decision | Rationale | Tradeoffs |
|----------|-----------|-----------|
| `{decision}` | `{rationale}` | `{tradeoffs}` |

## External Integrations

| System | Protocol | Module | Purpose |
|--------|----------|--------|---------|
| `{system}` | `{protocol}` | `{module}` | `{purpose}` |

---

_Generated by wiki-generator — Last updated: {YYYY-MM-DD}_
