# Module Reference — `{module_name}`

> Evidence sources: `{graph_mcp_node_ids}`, `{filesystem_paths}`

## Overview

- **Purpose**: `{module_purpose}`
- **Type**: `{service|controller|repository|utility|domain}`
- **Location**: `{file_path}`
- **Key Symbols**: `{class_count}` classes, `{function_count}` functions

## Dependencies

### Internal Dependencies
| Module | Relationship | Details |
|--------|-------------|---------|
| `{dep_module}` | `{calls|imports|extends}` | `{details}` |

### External Dependencies
| Package | Version | Purpose |
|---------|---------|---------|
| `{package}` | `{version}` | `{purpose}` |

## Key Classes and Functions

| Name | Type | Visibility | Purpose |
|------|------|-----------|---------|
| `{name}` | `{class|function|interface}` | `{public|private}` | `{purpose}` |

## Entry Points

| Entry Point | Signature | Description |
|-------------|-----------|-------------|
| `{name}` | `{signature}` | `{description}` |

## API Endpoints (if applicable)

| Method | Path | Handler | Description |
|--------|------|---------|-------------|
| `{method}` | `{path}` | `{handler}` | `{description}` |

## Data Models

| Model | Fields | Source |
|-------|--------|--------|
| `{model}` | `{fields}` | `{file}` |

## Configuration

| Config Key | Default | Description |
|------------|---------|-------------|
| `{key}` | `{default}` | `{description}` |

## Cross-References

- [API Reference — {module_name}](../api/{module_name}-api.md)
- Back to [Architecture Overview](../architecture-overview.md)

---

_Generated by wiki-generator — Evidence confidence: {confidence}_
